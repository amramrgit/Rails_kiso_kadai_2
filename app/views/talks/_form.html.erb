<%= form_with(model: @talk, local: true, url: choose_new_or_edit) do |form| %>

  <div class="talk_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>

<%= link_to "一覧画面にもどる", talks_path %>
